package view;

/**
 * Interface for the graphical user interface (GUI).
 */
public interface StocksView {

  /**
   * Adds a message to the GUI.
   * @param str String to add to the GUI
   */
  void write(String str);
}
