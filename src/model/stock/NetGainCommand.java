package model.stock;

/**
 * Command to get the net gain of a stock.
 */
public class NetGainCommand implements StockCommand {

  /**
   * Executes the command onto a {@link Stock} object.
   *
   * @param stock {@link Stock} object
   */
  @Override
  public void execute(Stock stock) {

  }
}
