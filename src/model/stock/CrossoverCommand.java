package model.stock;

/**
 * Command to get the x-days crossover.
 */
public class CrossoverCommand implements StockCommand {

  /**
   * Executes the command onto a {@link Stock} object.
   *
   * @param stock {@link Stock} object
   */
  @Override
  public void execute(Stock stock) {

  }
}
