package controller;

/**
 * Enum class that represents all the possible states of the program for the controller
 * to handle.
 */
public enum ControllerState {
  START_MENU, PORTFOLIO_MENU, SPECIFIC_PORTFOLIO_MENU, STOCK_MENU, QUIT
}
